name: Phase 8 Dashboards & Visual Showcase CI

on:
  push:
    branches: [ main, feature/* ]
  pull_request:
    branches: [ main ]

jobs:
  validate-dashboards-showcase:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: gradment_dw_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install psycopg2-binary pyyaml

      - name: Validate Phase 1 Event Catalog Contract
        run: |
          python scripts/validate_events_catalog.py

      - name: Validate Phase 3 Star Schema Contract
        run: |
          python scripts/validate_star_schema.py

      - name: Verify Native PostgreSQL DDL & EXPLAIN Partition Pruning
        env:
          PG_HOST: localhost
          PG_PORT: 5432
          PG_USER: postgres
          PG_PASS: postgres
          PG_DB: gradment_dw_test
        run: |
          python scripts/test_postgres_execution_and_pruning.py

      - name: Validate Phase 4 Pipeline Execution
        env:
          PG_HOST: localhost
          PG_PORT: 5432
          PG_USER: postgres
          PG_PASS: postgres
          PG_DB: gradment_dw_test
        run: |
          python scripts/validate_phase4_pipeline.py

      - name: Validate Phase 5 Data Quality Suite
        env:
          PG_HOST: localhost
          PG_PORT: 5432
          PG_USER: postgres
          PG_PASS: postgres
          PG_DB: gradment_dw_test
        run: |
          python scripts/validate_phase5_quality.py

      - name: Validate Phase 6 Data Warehouse Performance
        env:
          PG_HOST: localhost
          PG_PORT: 5432
          PG_USER: postgres
          PG_PASS: postgres
          PG_DB: gradment_dw_test
        run: |
          python scripts/validate_phase6_warehouse.py

      - name: Verify Section 19 Metric Drift (56 Metrics across 9 Categories)
        run: |
          python scripts/check_metrics_drift.py

      - name: Validate Phase 7 Metrics Calculations
        env:
          PG_HOST: localhost
          PG_PORT: 5432
          PG_USER: postgres
          PG_PASS: postgres
          PG_DB: gradment_dw_test
        run: |
          python scripts/validate_phase7_metrics.py

      - name: Validate Phase 8 Role-Based Dashboards & Visual Showcase
        env:
          PG_HOST: localhost
          PG_PORT: 5432
          PG_USER: postgres
          PG_PASS: postgres
          PG_DB: gradment_dw_test
        run: |
          python scripts/generate_dashboards.py
          python scripts/generate_dashboard_screenshots.py
          python scripts/validate_phase8_dashboards.py
