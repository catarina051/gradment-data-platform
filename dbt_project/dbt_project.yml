name: 'gradment_data_platform'
version: '1.0.0'
config-version: 2

profile: 'gradment_dw'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  gradment_data_platform:
    staging:
      +materialized: view
    marts:
      core:
        +materialized: table
        fct_events:
          +materialized: incremental
          +unique_key: 'event_id'
          +incremental_strategy: delete+insert
        fct_daily_user_activity:
          +materialized: table
        fct_ratings:
          +materialized: table
        fct_sessions:
          +materialized: table
        dim_users:
          +materialized: table
        dim_professors:
          +materialized: table
        dim_courses:
          +materialized: table
        dim_universities:
          +materialized: table
        dim_academic_periods:
          +materialized: table
        dim_date:
          +materialized: table
        dim_screens:
          +materialized: table
